# initiative-ties: The GM orders tied monsters, the players tied characters, the GM a mixed tie

<!-- rules-factory-entry: initiative-ties -->
<!-- rules-factory-engine: Srd52Combat; map: RulesFactory.Maps.Srd52Combat -->

Backlog item 52 of 70 for Srd52Combat, from RulesFactory.Maps.Srd52Combat 1.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Corpus licence

Text quoted here from corpus `srd-5.2.1` (its evidence, and any note or question that quotes it) is under the corpus's licence, CC-BY-4.0, not this repository's. The full terms, and what was changed, are in `LICENCE.txt` inside RulesFactory.Maps.Srd52Combat 1.0.0 (rules-factory decision 0023). The attribution the licence requires, verbatim:

This work includes material from the System Reference Document 5.2.1 (“SRD 5.2.1”) by Wizards of the Coast LLC, available at https://www.dndbeyond.com/srd. The SRD 5.2.1 is licensed under the Creative Commons Attribution 4.0 International License, available at https://creativecommons.org/licenses/by/4.0/legalcode.

## Entry

- id: `initiative-ties`
- kind: `assertion`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "srd-5.2.1"; citation:

```text
Combat / Initiative / p. 13
```

Evidence, verbatim:

```text
Ties. If a tie occurs, the GM decides the order among tied monsters, and the players decide the order among tied characters. The GM decides the 13 System Reference Document 5.2.1 order if the tie is between a monster and a player character.
```

## Dependencies

`dependsOn` -- built before this item:

- [`initiative-order`](049-initiative-order.md)

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- none

## Acceptance criteria

- [ ] The engine demands this fact of the caller rather than deciding it; what the caller states is what the engine evaluates.
- [ ] A hand-written handler implements `Handlers.InitiativeTies`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `initiative-ties` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
An assertion under gate 3: the decider's own determination is operative and the corpus fixes the set of values, “the order among tied” combatants, which is the orderings of exactly those combatants and nothing else. The engine demands the order, attributes it (to the GM or to the players, which the map has no field to record), records it and never infers it. The span carries “13 System Reference Document 5.2.1” mid-sentence: the printed folio and running header, which pdftotext emits between the page's two columns (README, findings). The cases it does not assign are initiative-ties-uncovered.
```
