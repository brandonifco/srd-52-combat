# cover-bonuses: Half Cover gives +2, Three-Quarters Cover +5, to AC and Dexterity saving throws

<!-- rules-factory-entry: cover-bonuses -->
<!-- rules-factory-engine: Srd52Combat; map: RulesFactory.Maps.Srd52Combat -->

Backlog item 15 of 66 for Srd52Combat, from RulesFactory.Maps.Srd52Combat 2.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Corpus licence

Text quoted here from corpus `srd-5.2.1` (its evidence, and any note or question that quotes it) is under the corpus's licence, CC-BY-4.0, not this repository's. The full terms, and what was changed, are in `LICENCE.txt` inside RulesFactory.Maps.Srd52Combat 2.0.0 (rules-factory decision 0023). The attribution the licence requires, verbatim:

This work includes material from the System Reference Document 5.2.1 (“SRD 5.2.1”) by Wizards of the Coast LLC, available at https://www.dndbeyond.com/srd. The SRD 5.2.1 is licensed under the Creative Commons Attribution 4.0 International License, available at https://creativecommons.org/licenses/by/4.0/legalcode.

## Entry

- id: `cover-bonuses`
- kind: `value`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "srd-5.2.1"; citation:

```text
Combat / Cover / p. 15
```

Evidence, verbatim:

```text
Degree Benefit to Target Offered By … Half +2 bonus to AC and Dexterity saving throws Another creature or an object that covers at least half of the target Three+5 bonus to AC Quarters and Dexterity saving throws An object that covers at least three-quarters of the target Total An object that covers the whole target Can’t be targeted directly
```

## Dependencies

`dependsOn` -- built before this item:

- none

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- none

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- none

## Acceptance criteria

- [ ] The engine states this value as the evidence prints it -- every figure and unit, and where the corpus prints a finite table, the whole table.
- [ ] A hand-written handler implements `Handlers.CoverBonuses`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `cover-bonuses` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
The whole table, never a sample, and transcribed from the printed page, not from this span. The span is the table as pdftotext emits it, and it is not the table as printed: the Three-Quarters row's label is split across its cell break, so the extraction reads “Three+5 bonus to AC Quarters and Dexterity saving throws”, and the Total row's two cells come out in the opposite order to the printed columns (“An object that covers the whole target” before “Can’t be targeted directly”). The quote is verbatim of the extraction, which is what the checker can hold it to; that the printed row reads Three-Quarters / +5 bonus to AC and Dexterity saving throws was read from the rendered page, and the whole table as read is extraction.renderedReading (docs/decisions/0024), which no check verifies. The Total row is total-cover.
```
