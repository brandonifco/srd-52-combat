# falling-off: A DC 10 Dexterity save or fall off Prone when moved, knocked Prone, or the mount is

<!-- rules-factory-entry: falling-off -->
<!-- rules-factory-engine: Srd52Combat; map: RulesFactory.Maps.Srd52Combat -->

Backlog item 25 of 32 for Srd52Combat, from RulesFactory.Maps.Srd52Combat 2.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Corpus licence

Text quoted here from corpus `srd-5.2.1` (its evidence, and any note or question that quotes it) is under the corpus's licence, CC-BY-4.0, not this repository's. The full terms, and what was changed, are in `LICENCE.txt` inside RulesFactory.Maps.Srd52Combat 2.0.0 (rules-factory decision 0023). The attribution the licence requires, verbatim:

This work includes material from the System Reference Document 5.2.1 (“SRD 5.2.1”) by Wizards of the Coast LLC, available at https://www.dndbeyond.com/srd. The SRD 5.2.1 is licensed under the Creative Commons Attribution 4.0 International License, available at https://creativecommons.org/licenses/by/4.0/legalcode.

## Entry

- id: `falling-off`
- kind: `operation`
- clarity: `ambiguous`
- status: `mapped`

## Source

Locator, `sourceId` "srd-5.2.1"; citation:

```text
Combat / Falling Off / p. 16
```

Evidence, verbatim:

```text
If an effect is about to move your mount against its will while you’re on it, you must succeed on a DC 10 Dexterity saving throw or fall off, landing with the Prone condition (see “Rules Glossary”) in an unoccupied space within 5 feet of the mount. While mounted, you must make the same save if you’re knocked Prone or the mount is.
```

## Ambiguity

Fate: `unresolved`; unresolvedReason: `RequiresInterpretation`.

```text
Who chooses the unoccupied space within 5 feet of the mount the rider lands in, and what happens if there is none? The corpus states neither.
```

## Dependencies

`dependsOn` -- built before this item:

- `saving-throws` -- no backlog item (scope: out -- the engine answers OutsideCurrentScope)
- `prone-condition` -- no backlog item (scope: out -- the engine answers OutsideCurrentScope)

## Reachability

`enabledBy` -- what the tests must set up:

- [`mount-eligibility`](020-mount-eligibility.md)
- [`mounting-cost`](021-mounting-cost.md)

`suspendedBy` -- what the tests must show does not happen:

- none

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- `cites` "(see “Rules Glossary”)" -- `resolvedBy` `prone-condition` -- no backlog item (scope: out -- the engine answers OutsideCurrentScope)

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] Every test of the rule sets up a state in which `mount-eligibility` or `mounting-cost` holds, and a test shows the rule does not apply where none of them holds.
- [ ] Where the answer turns on the question above, the engine declines with `RequiresInterpretation` citing this entry's locator; it does not choose a reading.
- [ ] A hand-written handler implements `Handlers.FallingOff`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `falling-off` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
The save is saving-throws, outside the extent. enabledBy mounting-cost as well as mount-eligibility: a rider falls off only after mounting (blind-mapping/resolutions.json). The save is a D20 Test: “the game uses a d20 roll to determine success or failure. These rolls are called D20 Tests, and they come in three kinds: ability checks, saving throws, and attack rolls” (Playing the Game / D20 Tests / p. 6) (0025).
```
