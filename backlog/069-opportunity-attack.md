# opportunity-attack: Making an Opportunity Attack when a seen creature leaves your reach

<!-- rules-factory-entry: opportunity-attack -->
<!-- rules-factory-engine: Srd52Combat; map: RulesFactory.Maps.Srd52Combat -->

Backlog item 69 of 70 for Srd52Combat, from RulesFactory.Maps.Srd52Combat 1.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Corpus licence

Text quoted here from corpus `srd-5.2.1` (its evidence, and any note or question that quotes it) is under the corpus's licence, CC-BY-4.0, not this repository's. The full terms, and what was changed, are in `LICENCE.txt` inside RulesFactory.Maps.Srd52Combat 1.0.0 (rules-factory decision 0023). The attribution the licence requires, verbatim:

This work includes material from the System Reference Document 5.2.1 (“SRD 5.2.1”) by Wizards of the Coast LLC, available at https://www.dndbeyond.com/srd. The SRD 5.2.1 is licensed under the Creative Commons Attribution 4.0 International License, available at https://creativecommons.org/licenses/by/4.0/legalcode.

## Entry

- id: `opportunity-attack`
- kind: `operation`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "srd-5.2.1"; citation:

```text
Combat / Opportunity Attacks / p. 15
```

Evidence, verbatim:

```text
Making an Opportunity Attack. You can make an Opportunity Attack when a creature that you can see leaves your reach. To make the attack, take a Reaction to make one melee attack with a weapon or an Unarmed Strike against that creature. The attack occurs right before it leaves your reach.
```

## Dependencies

`dependsOn` -- built before this item:

- [`melee-within-reach`](024-melee-within-reach.md)
- `reactions` -- no backlog item (scope: out -- the engine answers OutsideCurrentScope)
- [`attack-structure`](068-attack-structure.md)

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- [`opportunity-attack-avoidance`](035-opportunity-attack-avoidance.md)
- `reactions` -- no backlog item (scope: out -- the engine answers OutsideCurrentScope)
- `incapacitated-condition` -- no backlog item (scope: out -- the engine answers OutsideCurrentScope)

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] A test shows the rule does not apply while `opportunity-attack-avoidance` holds.
- [ ] A test shows the rule does not apply while `reactions` holds.
- [ ] A test shows the rule does not apply while `incapacitated-condition` holds.
- [ ] A hand-written handler implements `Handlers.OpportunityAttack`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `opportunity-attack` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
Timing is stated: before the creature leaves reach, which is the case reactions' default (“immediately after its trigger unless the Reaction’s description says otherwise”, p. 10) allows. The Rules Glossary restates the trigger with a list of the ways of leaving (opportunity-attacks-glossary), consistent with this entry and opportunity-attack-avoidance read together. Suspended by opportunity-attack-avoidance, and by reactions while a Reaction has already been taken since the start of the attacker's last turn. Also suspended by incapacitated-condition: the attack is made with a Reaction, which an Incapacitated creature cannot take (blind-mapping/resolutions.json).
```
