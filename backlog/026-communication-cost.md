# communication-cost: Brief communication is free; extended communication takes an action

<!-- rules-factory-entry: communication-cost -->
<!-- rules-factory-engine: Srd52Combat; map: RulesFactory.Maps.Srd52Combat -->

Backlog item 26 of 66 for Srd52Combat, from RulesFactory.Maps.Srd52Combat 2.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Corpus licence

Text quoted here from corpus `srd-5.2.1` (its evidence, and any note or question that quotes it) is under the corpus's licence, CC-BY-4.0, not this repository's. The full terms, and what was changed, are in `LICENCE.txt` inside RulesFactory.Maps.Srd52Combat 2.0.0 (rules-factory decision 0023). The attribution the licence requires, verbatim:

This work includes material from the System Reference Document 5.2.1 (“SRD 5.2.1”) by Wizards of the Coast LLC, available at https://www.dndbeyond.com/srd. The SRD 5.2.1 is licensed under the Creative Commons Attribution 4.0 International License, available at https://creativecommons.org/licenses/by/4.0/legalcode.

## Entry

- id: `communication-cost`
- kind: `operation`
- clarity: `ambiguous`
- status: `mapped`

## Source

Locator, `sourceId` "srd-5.2.1"; citation:

```text
Combat / Your Turn / p. 13
```

Evidence, verbatim:

```text
Communicating. You can communicate however you are able—through brief utterances and gestures—as you take your turn. Doing so uses neither your action nor your move. Extended communication, such as a detailed explanation of something or an attempt to persuade a foe, requires an action.
```

## Ambiguity

Fate: `unresolved`; unresolvedReason: `RequiresInterpretation`.

```text
Does gm-requires-action reach communication? “The GM might require you to use an action for any of these activities” follows the “Interacting with Things” paragraphs on the printed page, and both of its instances (a stuck door, a crank) are object interactions, but “these activities” has no stated antecedent and “Your Turn” also describes free communication. If it reaches communication, the GM may make brief communication cost an action; if not, never.
```

## Dependencies

`dependsOn` -- built before this item:

- [`brief-or-extended-communication`](003-brief-or-extended-communication.md)
- `actions-table` -- no backlog item (scope: out -- the engine answers OutsideCurrentScope)

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- [`gm-requires-action`](028-gm-requires-action.md)

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] A test shows the rule does not apply while `gm-requires-action` holds.
- [ ] Where the answer turns on the question above, the engine declines with `RequiresInterpretation` citing this entry's locator; it does not choose a reading.
- [ ] A hand-written handler implements `Handlers.CommunicationCost`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `communication-cost` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
Given the classification, the cost: none, or one action. The next sentence in the corpus (“The Influence action is the main way you try to influence a monster.”) is a pointer to actions-table and is not quoted. suspendedBy gm-requires-action is recorded on the reading that “any of these activities” reaches communication, which is the question. The first mapping said this in a note and called the entry clear; the blind second mapping asked it (blind-mapping/resolutions.json).
```
