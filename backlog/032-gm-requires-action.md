# gm-requires-action: The GM may require an action for an activity needing special care or presenting an unusual obstacle

<!-- rules-factory-entry: gm-requires-action -->
<!-- rules-factory-engine: Srd52Combat; map: RulesFactory.Maps.Srd52Combat -->

Backlog item 32 of 66 for Srd52Combat, from RulesFactory.Maps.Srd52Combat 1.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Corpus licence

Text quoted here from corpus `srd-5.2.1` (its evidence, and any note or question that quotes it) is under the corpus's licence, CC-BY-4.0, not this repository's. The full terms, and what was changed, are in `LICENCE.txt` inside RulesFactory.Maps.Srd52Combat 1.0.0 (rules-factory decision 0023). The attribution the licence requires, verbatim:

This work includes material from the System Reference Document 5.2.1 (“SRD 5.2.1”) by Wizards of the Coast LLC, available at https://www.dndbeyond.com/srd. The SRD 5.2.1 is licensed under the Creative Commons Attribution 4.0 International License, available at https://creativecommons.org/licenses/by/4.0/legalcode.

## Entry

- id: `gm-requires-action`
- kind: `assertion`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "srd-5.2.1"; citation:

```text
Combat / Your Turn / p. 14
```

Evidence, verbatim:

```text
The GM might require you to use an action for any of these activities when it needs special care or when it presents an unusual obstacle. For instance, the GM might require you to take the Utilize action to open a stuck door or turn a crank to lower a drawbridge.
```

## Dependencies

`dependsOn` -- built before this item:

- `actions-table` -- no backlog item (scope: out -- the engine answers OutsideCurrentScope)

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- none

## Acceptance criteria

- [ ] The engine demands this fact of the caller rather than deciding it; what the caller states is what the engine evaluates.
- [ ] A hand-written handler implements `Handlers.GmRequiresAction`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `gm-requires-action` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
An assertion under gate 3: the GM's own determination is operative, and the corpus states in the same constituent what it is measured against, “when it needs special care or when it presents an unusual obstacle”. The measure is itself open, which gate 3 does not ask about (0010). A gate, in suspendedBy, on the free interaction and on free communication; whether it reaches communication is communication-cost's question. The span sits on p. 14 after a page break and after the “Playing on a Grid” sidebar that pdftotext places between it and the paragraph it continues.
```
