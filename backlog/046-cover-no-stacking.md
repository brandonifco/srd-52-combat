# cover-no-stacking: Only the most protective degree of cover applies

<!-- rules-factory-entry: cover-no-stacking -->
<!-- rules-factory-engine: Srd52Combat; map: RulesFactory.Maps.Srd52Combat -->

Backlog item 46 of 55 for Srd52Combat, from RulesFactory.Maps.Srd52Combat 2.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Corpus licence

Text quoted here from corpus `srd-5.2.1` (its evidence, and any note or question that quotes it) is under the corpus's licence, CC-BY-4.0, not this repository's. The full terms, and what was changed, are in `LICENCE.txt` inside RulesFactory.Maps.Srd52Combat 2.0.0 (rules-factory decision 0023). The attribution the licence requires, verbatim:

This work includes material from the System Reference Document 5.2.1 (“SRD 5.2.1”) by Wizards of the Coast LLC, available at https://www.dndbeyond.com/srd. The SRD 5.2.1 is licensed under the Creative Commons Attribution 4.0 International License, available at https://creativecommons.org/licenses/by/4.0/legalcode.

## Entry

- id: `cover-no-stacking`
- kind: `operation`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "srd-5.2.1"; citation:

```text
Combat / Cover / p. 15
```

Evidence, verbatim:

```text
If a target is behind multiple sources of cover, only the most protective degree of cover applies; the degrees aren’t added together. For example, if a target is behind a creature that gives Half Cover and a tree trunk that gives Three-Quarters Cover, the target has Three-Quarters Cover.
```

## Dependencies

`dependsOn` -- built before this item:

- [`cover-degree`](014-cover-degree.md)
- `cover-glossary` -- no backlog item (scope: out -- the engine answers OutsideCurrentScope)

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- none

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- `cites` "only the most protective degree of cover applies" -- `resolvedBy` `cover-glossary` -- no backlog item (scope: out -- the engine answers OutsideCurrentScope)

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] A hand-written handler implements `Handlers.CoverNoStacking`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `cover-no-stacking` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
Demonstrate the corpus's own example: Half and Three-Quarters give Three-Quarters, not +7. The Rules Glossary's “Cover” (cover-glossary, p. 179) says it of a target “behind more than one degree of cover”, which also decides one object that qualifies for two degrees; dependsOn names it (0026).
```
