# total-cover: A target with Total Cover can't be targeted directly

<!-- rules-factory-entry: total-cover -->
<!-- rules-factory-engine: Srd52Combat; map: RulesFactory.Maps.Srd52Combat -->

Backlog item 15 of 42 for Srd52Combat, from RulesFactory.Maps.Srd52Combat 2.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Corpus licence

Text quoted here from corpus `srd-5.2.1` (its evidence, and any note or question that quotes it) is under the corpus's licence, CC-BY-4.0, not this repository's. The full terms, and what was changed, are in `LICENCE.txt` inside RulesFactory.Maps.Srd52Combat 2.0.0 (rules-factory decision 0023). The attribution the licence requires, verbatim:

This work includes material from the System Reference Document 5.2.1 (“SRD 5.2.1”) by Wizards of the Coast LLC, available at https://www.dndbeyond.com/srd. The SRD 5.2.1 is licensed under the Creative Commons Attribution 4.0 International License, available at https://creativecommons.org/licenses/by/4.0/legalcode.

## Entry

- id: `total-cover`
- kind: `operation`
- clarity: `ambiguous`
- status: `mapped`

## Source

Locator, `sourceId` "srd-5.2.1"; citation:

```text
Combat / Cover / p. 15
```

Evidence, verbatim:

```text
Degree Benefit to Target Offered By … Half +2 bonus to AC and Dexterity saving throws Another creature or an object that covers at least half of the target Three+5 bonus to AC Quarters and Dexterity saving throws An object that covers at least three-quarters of the target Total An object that covers the whole target Can’t be targeted directly
```

## Ambiguity

Fate: `unresolved`; unresolvedReason: `RequiresInterpretation`.

```text
“Can’t be targeted directly” does not say what indirect targeting is. An area effect whose point of origin is elsewhere is the evident case, and the rules for it are in “Spells” (outside the extent); within the slice, the word “directly” is not defined and no measure is stated.
```

## Dependencies

`dependsOn` -- built before this item:

- [`cover-degree`](014-cover-degree.md)

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- none

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- none

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] Where the answer turns on the question above, the engine declines with `RequiresInterpretation` citing this entry's locator; it does not choose a reading.
- [ ] A hand-written handler implements `Handlers.TotalCover`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `total-cover` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
Same span as cover-bonuses.
```
