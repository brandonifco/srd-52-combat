# grid-entering-square: Entering a square costs 1 square, or 2 for Difficult Terrain

<!-- rules-factory-entry: grid-entering-square -->
<!-- rules-factory-engine: Srd52Combat; map: RulesFactory.Maps.Srd52Combat -->

Backlog item 58 of 66 for Srd52Combat, from RulesFactory.Maps.Srd52Combat 2.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Corpus licence

Text quoted here from corpus `srd-5.2.1` (its evidence, and any note or question that quotes it) is under the corpus's licence, CC-BY-4.0, not this repository's. The full terms, and what was changed, are in `LICENCE.txt` inside RulesFactory.Maps.Srd52Combat 2.0.0 (rules-factory decision 0023). The attribution the licence requires, verbatim:

This work includes material from the System Reference Document 5.2.1 (“SRD 5.2.1”) by Wizards of the Coast LLC, available at https://www.dndbeyond.com/srd. The SRD 5.2.1 is licensed under the Creative Commons Attribution 4.0 International License, available at https://creativecommons.org/licenses/by/4.0/legalcode.

## Entry

- id: `grid-entering-square`
- kind: `operation`
- clarity: `ambiguous`
- status: `mapped`

## Source

Locator, `sourceId` "srd-5.2.1"; citation:

```text
Combat / Playing on a Grid / p. 13
```

Evidence, verbatim:

```text
Entering a Square. To enter a square, you must have enough movement left to pay for entering. It costs 1 square of movement to enter an unoccupied square that’s adjacent to your space (orthogonally or diagonally adjacent). A square of Difficult Terrain costs 2 squares to enter. Other effects might make a square cost even more.
```

## Ambiguity

Fate: `unresolved`; unresolvedReason: `RequiresInterpretation`.

```text
The rule prices an unoccupied adjacent square (1 square) and a square of Difficult Terrain (2). It does not price a square occupied by a creature whose space is not Difficult Terrain for you, an ally or a Tiny creature (creature-space-difficult-terrain), though moving-through-creatures lets you pass through it. Translating Speed in 5-foot segments (grid-speed-in-squares) suggests 1 square, but the rule's own word “unoccupied” leaves the case out.
```

## Dependencies

`dependsOn` -- built before this item:

- [`grid-square-size`](008-grid-square-size.md)
- [`grid-speed-in-squares`](041-grid-speed-in-squares.md)
- [`difficult-terrain`](056-difficult-terrain.md)
- [`difficult-terrain-cost`](057-difficult-terrain-cost.md)

## Reachability

`enabledBy` -- what the tests must set up:

- [`grid-play`](007-grid-play.md)

`suspendedBy` -- what the tests must show does not happen:

- none

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- `cites` "Other effects might make a square cost even more." -- `unmapped`: "Names no rule; the effects are wherever they are stated (spells, the Rules Glossary), outside the extent."

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] Every test of the rule sets up a state in which `grid-play` holds, and a test shows the rule does not apply where none of them holds.
- [ ] Where the answer turns on the question above, the engine declines with `RequiresInterpretation` citing this entry's locator; it does not choose a reading.
- [ ] A hand-written handler implements `Handlers.GridEnteringSquare`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `grid-entering-square` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
A diagonal step costs the same as an orthogonal one; demonstrate it. A square occupied by a creature that is neither Tiny nor an ally is Difficult Terrain (creature-space-difficult-terrain), so it costs 2. The occupied-but-not-difficult case is the question, raised by the blind second mapping (blind-mapping/resolutions.json). The grid form of difficult-terrain-cost.
```
