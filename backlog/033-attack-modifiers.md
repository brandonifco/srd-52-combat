# attack-modifiers: Determine Cover, Advantage or Disadvantage, and other modifiers

<!-- rules-factory-entry: attack-modifiers -->
<!-- rules-factory-engine: Srd52Combat; map: RulesFactory.Maps.Srd52Combat -->

Backlog item 33 of 66 for Srd52Combat, from RulesFactory.Maps.Srd52Combat 2.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Corpus licence

Text quoted here from corpus `srd-5.2.1` (its evidence, and any note or question that quotes it) is under the corpus's licence, CC-BY-4.0, not this repository's. The full terms, and what was changed, are in `LICENCE.txt` inside RulesFactory.Maps.Srd52Combat 2.0.0 (rules-factory decision 0023). The attribution the licence requires, verbatim:

This work includes material from the System Reference Document 5.2.1 (“SRD 5.2.1”) by Wizards of the Coast LLC, available at https://www.dndbeyond.com/srd. The SRD 5.2.1 is licensed under the Creative Commons Attribution 4.0 International License, available at https://creativecommons.org/licenses/by/4.0/legalcode.

## Entry

- id: `attack-modifiers`
- kind: `operation`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "srd-5.2.1"; citation:

```text
Combat / Making an Attack / p. 15
```

Evidence, verbatim:

```text
2: Determine Modifiers. The GM determines whether the target has Cover (see the next section) and whether you have Advantage or Disadvantage against the target. In addition, spells, special abilities, and other effects can apply penalties or bonuses to your attack roll.
```

## Dependencies

`dependsOn` -- built before this item:

- [`cover-degree`](016-cover-degree.md)
- `advantage-disadvantage` -- no backlog item (scope: out -- the engine answers OutsideCurrentScope)

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- none

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- `cites` "(see the next section)" -- `resolvedBy` [`cover-degree`](016-cover-degree.md)
- `cites` "spells, special abilities, and other effects can apply penalties or bonuses" -- `unmapped`: "Spells (p. 104 onward), features and other effects are outside the extent; each states its own modifier."

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] A hand-written handler implements `Handlers.AttackModifiers`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `attack-modifiers` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
“The GM determines” is not an assertion here. The corpus says how Cover is determined (the Cover table's “Offered By” column) and when Advantage or Disadvantage applies (unseen-target-disadvantage and the rest), so what the GM supplies is the facts those rules test, and 0010 rules that whose fact it is decides nothing. The SRD says “the GM determines” of many computable rules; see the README.
```
