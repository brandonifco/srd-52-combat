# mounting-cost: Mounting or dismounting costs half your Speed, rounded down

<!-- rules-factory-entry: mounting-cost -->
<!-- rules-factory-engine: Srd52Combat; map: RulesFactory.Maps.Srd52Combat -->

Backlog item 21 of 55 for Srd52Combat, from RulesFactory.Maps.Srd52Combat 2.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Corpus licence

Text quoted here from corpus `srd-5.2.1` (its evidence, and any note or question that quotes it) is under the corpus's licence, CC-BY-4.0, not this repository's. The full terms, and what was changed, are in `LICENCE.txt` inside RulesFactory.Maps.Srd52Combat 2.0.0 (rules-factory decision 0023). The attribution the licence requires, verbatim:

This work includes material from the System Reference Document 5.2.1 (“SRD 5.2.1”) by Wizards of the Coast LLC, available at https://www.dndbeyond.com/srd. The SRD 5.2.1 is licensed under the Creative Commons Attribution 4.0 International License, available at https://creativecommons.org/licenses/by/4.0/legalcode.

## Entry

- id: `mounting-cost`
- kind: `operation`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "srd-5.2.1"; citation:

```text
Combat / Mounting and Dismounting / p. 15
```

Evidence, verbatim:

```text
During your move, you can mount a creature that is within 5 feet of you or dismount. Doing so costs an amount of movement equal to half your Speed (round down). For example, if your Speed is 30 feet, you spend 15 feet of movement to mount a horse.
```

## Dependencies

`dependsOn` -- built before this item:

- [`movement-deduction`](010-movement-deduction.md)

## Reachability

`enabledBy` -- what the tests must set up:

- [`mount-eligibility`](020-mount-eligibility.md)

`suspendedBy` -- what the tests must show does not happen:

- none

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- none

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] Every test of the rule sets up a state in which `mount-eligibility` holds, and a test shows the rule does not apply where none of them holds.
- [ ] A hand-written handler implements `Handlers.MountingCost`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `mounting-cost` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
Demonstrate an odd Speed (25 feet costs 12), and that a creature with less movement left than the cost cannot mount or dismount: movement is deducted from Speed “until it is used up” (movement-deduction) and a creature moves “a distance equal to your Speed or less” (move-up-to-speed), so no rule lets a cost take movement below zero. The first mapping asked whether it could; the blind second mapping read it as clear, and the evidence agrees (blind-mapping/resolutions.json). On a grid see grid-speed-in-squares.
```
