# controlled-mount-turn: A controlled mount shares your Initiative and can only Dash, Disengage or Dodge

<!-- rules-factory-entry: controlled-mount-turn -->
<!-- rules-factory-engine: Srd52Combat; map: RulesFactory.Maps.Srd52Combat -->

Backlog item 50 of 66 for Srd52Combat, from RulesFactory.Maps.Srd52Combat 1.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Corpus licence

Text quoted here from corpus `srd-5.2.1` (its evidence, and any note or question that quotes it) is under the corpus's licence, CC-BY-4.0, not this repository's. The full terms, and what was changed, are in `LICENCE.txt` inside RulesFactory.Maps.Srd52Combat 1.0.0 (rules-factory decision 0023). The attribution the licence requires, verbatim:

This work includes material from the System Reference Document 5.2.1 (“SRD 5.2.1”) by Wizards of the Coast LLC, available at https://www.dndbeyond.com/srd. The SRD 5.2.1 is licensed under the Creative Commons Attribution 4.0 International License, available at https://creativecommons.org/licenses/by/4.0/legalcode.

## Entry

- id: `controlled-mount-turn`
- kind: `operation`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "srd-5.2.1"; citation:

```text
Combat / Controlling a Mount / p. 16
```

Evidence, verbatim:

```text
The Initiative of a controlled mount changes to match yours when you mount it. It moves on your turn as you direct it, and it has only three action options during that turn: Dash, Disengage, and Dodge. A controlled mount can move and act even on the turn that you mount it.
```

## Dependencies

`dependsOn` -- built before this item:

- `initiative-order` -- no backlog item (status: implemented)
- `actions-table` -- no backlog item (scope: out -- the engine answers OutsideCurrentScope)

## Reachability

`enabledBy` -- what the tests must set up:

- [`mount-control-requires-training`](028-mount-control-requires-training.md)
- [`mounting-cost`](027-mounting-cost.md)

`suspendedBy` -- what the tests must show does not happen:

- none

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] Every test of the rule sets up a state in which `mount-control-requires-training` or `mounting-cost` holds, and a test shows the rule does not apply where none of them holds.
- [ ] A hand-written handler implements `Handlers.ControlledMountTurn`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `controlled-mount-turn` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
The three actions are a closed set. The mount leaves its own place in initiative-order and acts on the rider's turn. Dash, Disengage and Dodge are actions-table. enabledBy mounting-cost: the Initiative change happens “when you mount it” (blind-mapping/resolutions.json).
```
