# single-range: A ranged attack can't target beyond its range

<!-- rules-factory-entry: single-range -->
<!-- rules-factory-engine: Srd52Combat; map: RulesFactory.Maps.Srd52Combat -->

Backlog item 18 of 66 for Srd52Combat, from RulesFactory.Maps.Srd52Combat 2.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Corpus licence

Text quoted here from corpus `srd-5.2.1` (its evidence, and any note or question that quotes it) is under the corpus's licence, CC-BY-4.0, not this repository's. The full terms, and what was changed, are in `LICENCE.txt` inside RulesFactory.Maps.Srd52Combat 2.0.0 (rules-factory decision 0023). The attribution the licence requires, verbatim:

This work includes material from the System Reference Document 5.2.1 (“SRD 5.2.1”) by Wizards of the Coast LLC, available at https://www.dndbeyond.com/srd. The SRD 5.2.1 is licensed under the Creative Commons Attribution 4.0 International License, available at https://creativecommons.org/licenses/by/4.0/legalcode.

## Entry

- id: `single-range`
- kind: `operation`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "srd-5.2.1"; citation:

```text
Combat / Range / p. 15
```

Evidence, verbatim:

```text
You can make ranged attacks only against targets within a specified range. If a ranged attack, such as one made with a spell, has a single range, you can’t attack a target beyond this range.
```

## Dependencies

`dependsOn` -- built before this item:

- none

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- none

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- none

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] A hand-written handler implements `Handlers.SingleRange`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `single-range` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
Whether an attack is ranged is stated by the weapon or spell and is a parameter here. The description that introduces it (“When you make a ranged attack, you fire a bow, hurl an axe, or otherwise send projectiles to strike a foe at a distance.”) is on p. 15, inside the extent, and states no rule of its own, as melee-within-reach's “typically uses” sentence does not. The first mapping's note placed it outside the extent (blind-mapping/resolutions.json).
```
