# creature-size-space: The Creature Size and Space table

<!-- rules-factory-entry: creature-size-space -->
<!-- rules-factory-engine: Srd52Combat; map: RulesFactory.Maps.Srd52Combat -->

Backlog item 11 of 42 for Srd52Combat, from RulesFactory.Maps.Srd52Combat 2.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Corpus licence

Text quoted here from corpus `srd-5.2.1` (its evidence, and any note or question that quotes it) is under the corpus's licence, CC-BY-4.0, not this repository's. The full terms, and what was changed, are in `LICENCE.txt` inside RulesFactory.Maps.Srd52Combat 2.0.0 (rules-factory decision 0023). The attribution the licence requires, verbatim:

This work includes material from the System Reference Document 5.2.1 (“SRD 5.2.1”) by Wizards of the Coast LLC, available at https://www.dndbeyond.com/srd. The SRD 5.2.1 is licensed under the Creative Commons Attribution 4.0 International License, available at https://creativecommons.org/licenses/by/4.0/legalcode.

## Entry

- id: `creature-size-space`
- kind: `value`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "srd-5.2.1"; citation:

```text
Combat / Creature Size / p. 14
```

Evidence, verbatim:

```text
Creature Size and Space Size Space (Feet) Space (Squares) Tiny 2½ by 2½ feet 4 per square Small 5 by 5 feet 1 square Medium 5 by 5 feet 1 square Large 10 by 10 feet 4 squares (2 by 2) Huge 15 by 15 feet 9 squares (3 by 3) Gargantuan 20 by 20 feet 16 squares (4 by 4)
```

## Dependencies

`dependsOn` -- built before this item:

- `size-categories` -- no backlog item (status: implemented)
- `grid-square-size` -- no backlog item (status: implemented)

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- none

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- none

## Acceptance criteria

- [ ] The engine states this value as the evidence prints it -- every figure and unit, and where the corpus prints a finite table, the whole table.
- [ ] A hand-written handler implements `Handlers.CreatureSizeSpace`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `creature-size-space` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
The whole table, all six rows, never a sample. pdftotext emits it one cell per line, and in this table row order and cell order survive, which the Cover table's do not (cover-bonuses). “4 per square” for Tiny is four Tiny creatures to a square, not four squares. The Space (Squares) column is in the unit grid-square-size defines (blind-mapping/resolutions.json).
```
